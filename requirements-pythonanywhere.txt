# Core dependencies for PythonAnywhere deployment.
# Optional biometric/ML packages are excluded to keep the install small.
# Gunicorn is not needed on PythonAnywhere and is intentionally omitted.
Django==4.2.13

djangorestframework==3.14.0
django-cors-headers==4.3.1
firebase-admin==6.4.0
python-decouple==3.8
Pillow==12.2.0
django-filter==23.5
twilio==8.10.0
google-auth==2.25.0
requests==2.31.0
stripe==6.0.0
